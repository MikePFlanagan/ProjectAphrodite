export function CharacterAvatar({ name, gradient, size = 'md' }: { name: string; gradient: string; size?: 'sm' | 'md' | 'lg' }) { const sizes = { sm: 'size-10 text-lg', md: 'size-14 text-2xl', lg: 'size-24 text-4xl' }; return <div className={`grid shrink-0 place-items-center rounded-2xl bg-gradient-to-br font-semibold text-white shadow-lg ${gradient} ${sizes[size]}`}>{name.slice(0, 1)}</div>; }
